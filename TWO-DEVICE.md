پروزه اتوماسیون بورسی

# کار روی کامپیوتر محل کار و لپ‌تاپ منزل

هر دستگاه نسخهٔ مستقلی از پروژه دارد. مسیر فعلی و ترجیحی F:\Automasion است؛ کدهای اجرایی مسیر ریشه را از محل فایل خود پیدا می‌کنند و به نام کاربری ویندوز وابسته نیستند. اگر دستگاه دوم درایو F ندارد، پیش از انتخاب مسیر دیگری محل موردنظر مالک پروژه مشخص شود.

## راه‌اندازی هر دستگاه

نسخهٔ مبنا برای یکسان‌بودن محیط دو دستگاه Python 3.12 است. Git و مرورگر نیز لازم‌اند. پس از دریافت مخزن، در PowerShell دستور زیر اجرا شود؛ این فرمان محیط مجازی را محلی می‌سازد و تنظیمات موجود را بازنویسی نمی‌کند:

```powershell
.\setup-local.ps1
```

اگر Python 3.12 مسیر دیگری دارد، آن را با پارامتر `-Python` بدهید. برای نصب وابستگی‌های ابزار مستندسازی، گزینهٔ `-InstallDocumentTools` اضافه شود. بسته‌های برنامهٔ اصلی پس از انتخاب فناوری جداگانه ثبت خواهند شد. فایل وابستگی فعلی مربوط به ابزار سند است و قفل وابستگی کل برنامه نیست.

```powershell
.\.venv\Scripts\python.exe .\check-local.py
```

اسکریپت بررسی از کتابخانهٔ استاندارد استفاده می‌کند. `config.local.toml` مخصوص همان دستگاه است؛ نام دستگاه را در آن به work یا home تغییر دهید. میزبان `127.0.0.1` و پورت پیش‌فرض `8765` است. نسخهٔ اولیهٔ سرور ساخته شده و فقط روی همان دستگاه اجرا می‌شود.

برای اجرای نسخهٔ اولیه در هر دستگاه:

```powershell
.\.venv\Scripts\python.exe .\backend\server.py
```

سپس `http://127.0.0.1:8765` را باز کنید. در این مرحله فقط گزارش نمونهٔ غیرشخصی نمایش داده می‌شود.

## جابه‌جایی کار توسعه

قبل از شروع کار، تغییرات محلی را با `git status` بررسی کنید. اگر تغییر ناتمام دارید، ابتدا آن را تعیین تکلیف کنید. سپس `git pull --ff-only` اجرا شود. در صورت تعارض یا شاخه‌های واگرا، توقف و بررسی انجام شود؛ از بازنویسی اجباری یا حذف تغییرات استفاده نشود.

پایان هر مرحله: خروجی‌ها بررسی شوند، `git diff` دیده شود، فقط فایل‌های مرتبط و فاقد دادهٔ حساس به stage اضافه شوند، `git diff --cached` بررسی شود، سپس commit و push انجام شود. ارسال موفق شرط تحویل مرحله به دستگاه دیگر است. هنگام نبود اینترنت، commit محلی هنوز روی GitHub یا دستگاه دوم در دسترس نیست.

روی یک مرحله یا سند Word هم‌زمان در دو دستگاه کار نشود. فایل‌های Markdown مرجع قابل‌مقایسهٔ تصمیم‌ها هستند و Word خروجی قابل‌تحویل است؛ اختلاف Word با انتخاب نسخهٔ درست و بازتولید حل شود، نه ادغام خودکار.

## فایل مشترک و فایل محلی

کد، اسکیل‌ها و ترجمه‌ها، مستندات عمومی پروژه، فایل تنظیمات نمونه و مشخصات وابستگی‌ها در GitHub نگهداری شوند. `config.local.toml`، `.venv`، اطلاعات ورود، کلیدها، نشست مرورگر، دادهٔ واقعی پرتفوی، گزارش شخصی، پایگاه داده، لاگ، فایل‌های موقت و پشتیبان در Git قرار نگیرند. `.gitignore` برای این تفکیک اضافه شده است؛ قبل از هر ارسال محتوا نیز باید بازبینی شود.

هر دستگاه ورود مستقل به کارگزاری دارد. کوکی و پروفایل مرورگر منتقل نشوند. محیط `.venv` روی هر دستگاه دوباره ساخته شود. اسکیل‌های موجود در پوشهٔ پروژه به‌تنهایی در ابزار هوش مصنوعی دستگاه دوم نصب نمی‌شوند؛ شناسایی یا نصب محلی آن‌ها مرحلهٔ جداگانه است.

## استفاده و داده‌های خصوصی

شروع نسخهٔ اول دستی و استفاده به‌صورت نوبتی روی یکی از دو دستگاه است. پایگاه داده و تاریخچهٔ گزارش‌ها به‌طور خودکار مشترک نیستند. دریافت تازهٔ پرتفوی روی دستگاه فعال انجام می‌شود. نیاز به تاریخچهٔ مشترک با قابلیت خروجی/ورودی رمزگذاری‌شده در مرحلهٔ بعد پیاده‌سازی خواهد شد؛ این قابلیت فعلاً وجود ندارد.

پایگاه دادهٔ در حال استفاده از طریق پوشهٔ همگام‌شونده منتقل نشود. اگر انتقال دستی داده بعداً لازم شد، برنامه بسته، پشتیبان تهیه و نسخهٔ ساختار داده بررسی شود. کلید رمزگذاری کنار خروجی منتقل نشود.

## وضعیت و آزمون تحویل

مخزن GitHub پروژه https://github.com/bahrami05-meh/Automation.git است و شاخهٔ پایه main است. وضعیت ارسال با مقایسهٔ commit محلی و راه دور تأیید شود. راه‌اندازی روی لپ‌تاپ منزل هنوز آزموده نشده است. هستهٔ پایتون و داشبورد نمونه پیاده‌سازی شده‌اند؛ مدیریت نشست و دریافت واقعی پرتفوی هنوز پیاده‌سازی نشده‌اند.

پذیرش نهایی دو دستگاه: دریافت یک commit یکسان، راه‌اندازی جداگانه، بازشدن داشبورد از localhost، ورود مستقل، تولید گزارش با دادهٔ آزمایشی یکسان و خروجی محاسباتی یکسان، و اطمینان از نبود اطلاعات خصوصی در فایل‌های stageشده. تنظیمات مدل و نسخهٔ قواعد نیز باید یکسان باشند؛ متن تولیدی مدل لزوماً کلمه‌به‌کلمه یکسان نیست.
