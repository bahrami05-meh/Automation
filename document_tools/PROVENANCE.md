پروزه اتوماسیون بورسی

فایل fa_docx.py بدون تغییر از scripts/fa_docx.py در افزونهٔ documents نسخهٔ 26.722.10000 موجود روی این دستگاه کپی شده است تا ابزار سند به مسیر نصب و نام کاربری یک دستگاه وابسته نباشد. این فایل توابع فارسی و راست‌به‌چپ python-docx را تأمین می‌کند. منبع: بستهٔ محلی gapgpt/documents. فایل اصلی افزونه تغییر نکرده است.
