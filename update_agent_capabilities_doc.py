# پروزه اتوماسیون بورسی
from pathlib import Path
from docx import Document
from build_jack_project_doc import add_body, add_heading, add_table

root = Path(__file__).resolve().parent
path = root / 'مستند-پروژه-جک.docx'
doc = Document(path)
title = '۱۵. توانایی‌ها و حدود عملکرد خودکار ایجنت‌ها'
if not any(p.text == title for p in doc.paragraphs):
    backup = root / '_label_backups' / 'before-agent-capabilities.docx'
    if not backup.exists():
        backup.write_bytes(path.read_bytes())
    add_heading(doc, title)
    add_body(doc, 'ایجنت‌ها پس از پیاده‌سازی هستهٔ پایتون می‌توانند در چارچوب مأموریت، ابزار و منبع دادهٔ تعیین‌شده کارهای تکراری را خودکار انجام دهند. در وضعیت فعلی فقط نقش‌ها و اسکیل‌های راهنما موجودند؛ ایجنت اجرایی، سرور محلی، داشبورد و دریافت واقعی پرتفوی هنوز ساخته نشده‌اند.')
    add_table(doc, ['فعالیت', 'وضعیت هدف', 'شرط یا محدودیت'], [
        ['انتخاب اندیکاتور', 'خودکار', 'از فهرست مشخص مانند RSI، MACD، میانگین متحرک، ایچیموکو، ATR و حجم بر اساس نوع داده و تایم‌فریم؛ دلیل انتخاب ثبت شود.'],
        ['مراجعه به منابع وب', 'خودکار', 'فقط منابع تأییدشدهٔ پروژه؛ تغییر ساختار سایت باید خطا و توقف کنترل‌شده ایجاد کند.'],
        ['ورود به کارگزاری', 'نیمه‌خودکار', 'جک صفحه را باز می‌کند؛ نام کاربری، رمز، CAPTCHA و کد تأیید را کاربر وارد می‌کند.'],
        ['استخراج پرتفوی', 'خودکار پس از ورود دستی', 'از جدول صفحه، فایل خروجی یا دادهٔ قابل‌مشاهده؛ منبع و زمان دریافت ثبت شوند.'],
        ['دریافت دادهٔ بازار', 'خودکار', 'از منابع مصوب، CSV، جدول HTML یا تصویر قابل‌خواندن؛ دادهٔ بی‌زمان، بی‌منبع یا ناسازگار تحلیل نمی‌شود.'],
        ['تحلیل تابلو و تکنیکال', 'خودکار', 'کنترل کیفیت OHLCV، وضعیت نماد، واحد ریال/تومان و تازگی داده پیش‌نیاز است.'],
        ['تحلیل بنیادی و رویدادها', 'پس از ساخت ایجنت', 'به ماژول اختصاصی و منابع رسمی صورت‌های مالی، افشا و صنعت نیاز دارد.'],
        ['تحلیل کل پرتفوی', 'پس از ساخت ایجنت', 'نیازمند وزن نمادها، صنعت، نقدشوندگی، دادهٔ ریسک و تاریخچه است.'],
        ['گزارش فارسی', 'پس از ساخت مدل جمع‌بند', 'باید واقعیت، محاسبه، برداشت و کمبود داده را تفکیک کند.'],
        ['خرید، فروش، برداشت یا انتقال وجه', 'خارج از دامنه', 'هیچ مسیر فنی برای اجرای اقدام مالی در پروژه ایجاد نمی‌شود.'],
    ], [2300, 2250, 4810], risk_rows={9})
    add_body(doc, 'گردش کار هدف: زمان‌بند محلی ← دریافت از مرورگر و منابع تأییدشده ← اعتبارسنجی ← ایجنت تابلو، تکنیکال، بنیادی، رویدادها و ریسک ← مدل جمع‌بند جک ← ذخیره و نمایش در index.html.')
    add_body(doc, 'محدودیت‌های پیش‌بینی‌شده: تغییر ساختار سایت، CAPTCHA، ورود دومرحله‌ای، قطع اینترنت و دادهٔ ناقص می‌توانند اجرای خودکار را متوقف کنند. در این شرایط وضعیت خطا یا DATA_BLOCKED گزارش می‌شود. API نداشتن سامانهٔ بورسی فقط به روش دریافت پرتفوی مربوط است و روش اجرای مدل هوش مصنوعی را تعیین نمی‌کند.')
    doc.save(path)
assert any(p.text == title for p in Document(path).paragraphs)
print('Agent capabilities section verified.')
