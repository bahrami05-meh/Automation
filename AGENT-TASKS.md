پروزه اتوماسیون بورسی

# قرارداد وظایف ایجنت‌ها

هر ایجنت فقط در محدودهٔ ورودی، منبع و ابزار تعیین‌شده عمل می‌کند. همهٔ خروجی‌ها باید شامل نماد، زمان، منبع، وضعیت کیفیت داده، شواهد، محدودیت‌ها و شناسهٔ اجرا باشند. اگر زمان، منبع، واحد قیمت یا دادهٔ ضروری نامشخص باشد، خروجی `DATA_BLOCKED` است.

## ۱. ایجنت دسترسی مرورگر و پرتفوی

پس از ورود دستی کاربر، صفحهٔ پرتفوی را فقط‌خواندنی باز می‌کند و جدول‌ها، فایل خروجی یا اطلاعات قابل‌مشاهده را استخراج می‌کند. ورود، رمز، CAPTCHA و احراز هویت دومرحله‌ای را کاربر انجام می‌دهد. نشست و اطلاعات ورود ذخیره یا به GitHub ارسال نمی‌شوند.

## ۲. ایجنت کنترل نمودار و اندیکاتور

### ورودی

- سایت نموداری تأییدشده، نماد و تایم‌فریم.
- اندیکاتور درخواستی کاربر یا اندیکاتور انتخاب‌شده از فهرست مجاز.
- پارامترهای مجاز مانند RSI 14، MACD 12/26/9، MA 20/50/200، ATR 14 یا ایچیموکو 9/26/52.

### وظایف

1. نماد و تایم‌فریم را در سایت باز کند.
2. اندیکاتور موردنظر را از رابط نمودار انتخاب و پارامترهای آن را اعمال کند.
3. نام سایت، نشانی صفحه، زمان دریافت، نماد، تایم‌فریم، نام اندیکاتور و پارامترها را ثبت کند.
4. مقدارهای قابل‌خواندن، دادهٔ نمودار یا تصویر صفحه را برای ایجنت تکنیکال ارسال کند.
5. اگر دادهٔ OHLCV معتبر موجود است، درخواست محاسبهٔ مستقل همان اندیکاتور در هستهٔ پایتون را ثبت کند.

### محدودیت‌ها و خطا

- فقط سایت‌ها، اندیکاتورها و پارامترهای تأییدشده قابل استفاده‌اند.
- انتخاب خودکار اندیکاتور باید دلیل خود را بر پایهٔ نوع تحلیل، تایم‌فریم و دادهٔ موجود ثبت کند.
- اگر منوی سایت تغییر کرده، نمودار خوانا نیست، سایت اندیکاتور را ندارد یا مقدارها ناسازگارند، نتیجه `DATA_BLOCKED` یا `SOURCE_MISMATCH` است.
- این ایجنت در صفحهٔ کارگزاری هیچ عملیاتی جز خواندن پرتفوی انجام نمی‌دهد و هرگز سفارش مالی ثبت نمی‌کند.

## ۳. ایجنت اعتبارسنجی داده

واحد ریال یا تومان، زمان، منبع، نوع قیمت خام یا تعدیل‌شده، سازگاری OHLCV، وضعیت نماد و تازگی داده را کنترل می‌کند. دادهٔ ناقص یا متناقض را به تحلیل نمی‌فرستد.

## ۴. ایجنت تکنیکال و تابلو

از دادهٔ معتبر، روند، حجم، حمایت/مقاومت، RSI، MACD، میانگین‌ها، ایچیموکو، ATR، واگرایی و اطلاعات حقیقی/حقوقی را تحلیل می‌کند. مقدارهای خوانده‌شده از نمودار را با محاسبهٔ مستقل مقایسه می‌کند و اختلاف را گزارش می‌دهد.

## ۵. ایجنت بنیادی و رویدادها

پس از ساخت ماژول‌های مربوط، صورت‌های مالی، نسبت‌ها، صنعت، افشاها، مجامع، توقف، افزایش سرمایه و رویدادهای شرکتی را فقط از منابع رسمی تأییدشده بررسی می‌کند.

## ۶. ایجنت ریسک پرتفوی

تمرکز سهم و صنعت، نقدشوندگی، سرمایهٔ آزاد، کارمزد، لغزش، بودجهٔ ریسک و اندازهٔ یک پلهٔ آزمایشی را بررسی می‌کند. سناریوی مغایر با محدودیت‌ها را `RISK_BLOCKED` اعلام می‌کند.

## ۷. ایجنت ناظر کیفیت و رفع خطا

این ایجنت آخرین دروازهٔ فنی پیش از مدل جمع‌بند جک است و خودش پیشنهاد خرید/فروش یا رأی تحلیلی تولید نمی‌کند. بستهٔ خروجی همهٔ ایجنت‌ها، دادهٔ خام، نسخهٔ قواعد امتیازدهی و خروجی ریسک را بررسی می‌کند تا دادهٔ اشتباه‌خوانده‌شده، واحد نادرست، زمان/منبع گمشده، تناقض بین ایجنت‌ها و امتیازدهی ناسازگار وارد گزارش نشود.

کنترل اجباری شامل بازاجرای قطعی فرمول امتیاز و وزن‌ها، بررسی آستانه‌ها و گردکردن عددی، تطبیق مقدارهای نمودار با محاسبهٔ مستقل از OHLCV، کنترل تازگی/خام یا تعدیل‌شده بودن قیمت، و تطبیق تصمیم پیشنهادی با محدودیت‌های ریسک است. ناظر همچنین اندیکاتورها را بر اساس خانوادهٔ سیگنال، دادهٔ ورودی، تایم‌فریم و پارامترها گروه‌بندی می‌کند تا اندیکاتورهای هم‌پوشان یک شاهد را چندبار در امتیاز تقویت نکنند. تعارض مؤثر با کد `INDICATOR_CONFLICT`، نام/پارامتر/تایم‌فریم هر اندیکاتور، جهت سیگنال و اثر عددی آن بر امتیاز اعلام می‌شود. اختلاف طبیعی تایم‌فریم‌ها با تعارض اشتباه گرفته نمی‌شود. ایجنت ناظر هرگز داده یا امتیاز را بی‌صدا اصلاح نمی‌کند؛ برای خطا کد، عامل مسئول و دادهٔ لازم ثبت می‌کند و فقط مرحلهٔ معیوب را برای بازاجرا برمی‌گرداند.

خروجی فقط یکی از `QA_PASSED`، `QA_RETRY` یا `QA_BLOCKED` است. فقط بستهٔ `QA_PASSED` به جک می‌رسد. بعد از سقف تلاش بازاجرا یا در نقص غیرقابل‌رفع، خروجی نهایی آن نماد باید `DATA_BLOCKED` یا «عدم اعلام نظر» باشد. گزارش ناظر شامل شناسهٔ اجرای کنترل، نسخهٔ قواعد، خطاهای کشف‌شده، اصلاحات ثبت‌شده و وضعیت نهایی کنترل است.

## ۸. مدل جمع‌بند جک

نتیجهٔ همهٔ ایجنت‌ها را با کیفیت داده و تعارض شواهد ترکیب می‌کند. برای هر نماد فقط یکی از «خرید آزمایشی یک‌پله‌ای»، «فروش آزمایشی یک‌پله‌ای»، «نگهداری / واچ‌لیست» یا «عدم اعلام نظر» را تولید می‌کند. خروجی باید دلیل، شواهد مخالف، سطح اطمینان، شرط تأیید، شرط ابطال و نیاز به تأیید انسانی را نشان دهد.

## ترتیب اجرا

ورود دستی کاربر ← دریافت پرتفوی و داده ← کنترل نمودار و اندیکاتور ← اعتبارسنجی ← تحلیل‌های تخصصی ← کنترل ریسک ← ایجنت ناظر کیفیت و رفع خطا ← مدل جمع‌بند جک ← ذخیره و نمایش محلی.

## تقویم بازار و زمان‌بند محلی

زمان‌بندی پایهٔ بازار سهام بورس و فرابورس ایران شنبه تا چهارشنبه، جلسهٔ اصلی ۰۹:۰۰ تا ۱۲:۳۰ به وقت `Asia/Tehran` است. پیش‌گشایش حدود ۰۸:۴۵ و مرحلهٔ معاملات پایانی برخی نمادها ۱۲:۴۵ تا ۱۳:۰۰ است. زمان‌بند باید فقط در پنجره‌های تعریف‌شده دریافت و تحلیل تازه انجام دهد. تعطیلات رسمی، توقف نماد، اطلاعیهٔ بازار و هر تغییر مقررات بر برنامهٔ پایه مقدم‌اند.

خارج از ساعت بازار، دادهٔ قبلی دادهٔ زنده نیست. ایجنت‌ها فقط می‌توانند گزارش‌های موجود را نمایش دهند یا اجرای بعدی را آماده کنند. اگر بازار بسته باشد، `MARKET_CLOSED` ثبت می‌شود؛ اگر وضعیت تقویم یا نماد قابل‌تأیید نباشد، `DATA_BLOCKED` ثبت می‌شود.
