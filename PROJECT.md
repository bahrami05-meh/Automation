پروزه اتوماسیون بورسی

تمام فایل‌های پروژه، اسناد، اسکیل‌ها، کدها، خروجی‌ها و نسخه‌های پشتیبان در F:\Automasion ذخیره شوند.

سند اصلی: مستند-پروژه-جک.docx

راهنمای توسعه و استفاده بین کامپیوتر محل کار و لپ‌تاپ منزل: TWO-DEVICE.md. ابزار آماده‌سازی هر دستگاه setup-local.ps1 و بررسی تنظیمات check-local.py است. فایل config.local.toml، محیط مجازی و داده‌های خصوصی به GitHub ارسال نمی‌شوند. این تمهیدات آماده‌سازی‌اند؛ اجرای خود سامانه هنوز ساخته نشده است.

## تصمیم‌های قطعی دریافت و اجرا

سامانهٔ بورسی طبق اعلام مالک پروژه API ندارد. دریافت پرتفوی از رابط مرورگر پس از ورود دستی کاربر انجام می‌شود. نام و آدرس سامانه هنوز لازم است. جزئیات روش استخراج بعد از مشاهدهٔ صفحه تعیین می‌شود.

کاربرد شخصی است و هر دو هسته روی کامپیوتر محل کار یا لپ‌تاپ منزل اجرا می‌شوند؛ سرور ابری یا رایانهٔ همیشه‌روشن در نیاز فعلی وجود ندارد. رابط از localhost باز می‌شود. تنظیمات اجرا باید قابل‌انتقال و مسیرها نسبت به ریشهٔ پروژه باشند؛ محل فعلی همهٔ فایل‌ها F:\Automasion است. ورود کاربر روی هر دستگاه جداگانه انجام می‌شود و نشست مرورگر بین دستگاه‌ها منتقل نمی‌شود.

ارتباط داخلی رابط HTML و هستهٔ پایتون مستقل از API سامانهٔ بورسی است. انتخاب مدل هوش مصنوعی و اجرای آن به‌صورت محلی یا از طریق API هنوز باز است. اجرای لوکال برنامه به معنی بی‌نیازی از اینترنت برای دریافت دادهٔ سایت نیست.

در پایان هر مرحله کدها، مستندات و اسکیل‌ها در GitHub ثبت و ارسال شوند. اطلاعات ورود، کلیدها، نشست‌ها و داده‌های شخصی پرتفوی از انتشار مستثنا باشند. مخزن معرفی‌شدهٔ مالک پروژه https://github.com/bahrami05-meh/Automation.git و شاخهٔ پایه main است. وضعیت واقعی ارسال با تاریخچهٔ Git و مخزن راه دور بررسی شود. انتقال کد بین دو دستگاه از انتقال داده‌های خصوصی مستقل است.

## معماری دو هسته‌ای

تصمیم مالک پروژه: هستهٔ اول برای جمع‌آوری و تحلیل با پایتون پیاده‌سازی می‌شود. هستهٔ دوم برای ارائهٔ اطلاعات، رابط HTML با صفحهٔ ورودی index.html روی سرور لوکال است.

هستهٔ اول مسئول دریافت و اعتبارسنجی داده، اجرای ایجنت‌های تحلیلی، جمع‌بندی مدل نهایی و ذخیرهٔ نتایج است. هستهٔ دوم مسئول نمایش وضعیت اجرا، اطلاعات پرتفوی و گزارش‌ها در مرورگر است. محاسبات تحلیلی در هستهٔ پایتون باقی می‌مانند.

طرح پیشنهادی ارتباط: مرورگر از طریق API محلی با هستهٔ پایتون ارتباط دارد و نتیجهٔ ساختاریافتهٔ JSON دریافت می‌کند. HTML ساختار نمایش، CSS ظاهر و JavaScript دریافت داده و تعامل را تأمین می‌کنند. index.html خودِ سرور نیست و توسط سرور محلی ارائه می‌شود.

پیشنهاد نسخهٔ اول: یک سرویس محلی، هم فایل‌های رابط کاربر و هم API را ارائه کند. دو هسته از نظر مسئولیت جدا هستند و الزاماً به دو سرور مستقل نیاز ندارند. چارچوب پایتون و پورت هنوز انتخاب نشده‌اند. منظور از لوکال فعلاً همان رایانه و دسترسی localhost است؛ دسترسی شبکه‌ای تصمیم جداگانه می‌خواهد.

ساختار پیشنهادی آینده: backend برای هستهٔ پایتون، frontend/index.html برای رابط، data برای داده‌ها، reports برای گزارش‌ها و skills برای اسکیل‌ها؛ همه زیر F:\Automasion. این ساختار فعلاً مستند شده و اجرای سامانه هنوز ساخته نشده است.

نسخه‌های کامل اسکیل‌های پروژه در پوشه skills قرار دارند. این نسخه‌ها از اسکیل‌های نصب‌شده کپی شده‌اند؛ نسخه‌های نصب‌شدهٔ برنامه در مسیر اصلی خود باقی می‌مانند.

عبارت شناسایی پروژه در اولین پاراگراف سند و اولین خط محتوای SKILL.md پس از فرادادهٔ YAML درج می‌شود تا ساختار شناسایی اسکیل سالم بماند.

## توانایی‌ها و حدود عملکرد خودکار ایجنت‌ها

ایجنت‌ها پس از پیاده‌سازی هستهٔ پایتون می‌توانند در چارچوب مأموریت، ابزار و منبع دادهٔ تعیین‌شده کارهای تکراری را خودکار انجام دهند. در وضعیت فعلی فقط نقش‌ها و اسکیل‌های راهنما موجودند؛ ایجنت اجرایی، سرور محلی، داشبورد و دریافت واقعی پرتفوی هنوز ساخته نشده‌اند.

| فعالیت | وضعیت هدف | شرط یا محدودیت |
| --- | --- | --- |
| انتخاب اندیکاتور | خودکار | انتخاب از فهرست مشخص مانند RSI، MACD، میانگین متحرک، ایچیموکو، ATR و حجم بر اساس نوع داده و تایم‌فریم؛ دلیل انتخاب ثبت شود. |
| مراجعه به منابع وب | خودکار | فقط منابع تأییدشدهٔ پروژه؛ تغییر ساختار سایت باید خطا و توقف کنترل‌شده ایجاد کند. |
| ورود به کارگزاری | نیمه‌خودکار | جک صفحه را باز می‌کند؛ نام کاربری، رمز، CAPTCHA و کد تأیید را کاربر وارد می‌کند. |
| استخراج پرتفوی | خودکار پس از ورود دستی | از جدول صفحه، فایل خروجی یا دادهٔ قابل‌مشاهده؛ منبع و زمان دریافت ثبت شوند. |
| دریافت دادهٔ بازار | خودکار | از منابع مصوب، CSV، جدول HTML یا تصویر قابل‌خواندن؛ دادهٔ بی‌زمان، بی‌منبع یا ناسازگار تحلیل نمی‌شود. |
| تحلیل تابلو و تکنیکال | خودکار | کنترل کیفیت OHLCV، وضعیت نماد، واحد ریال/تومان و تازگی داده پیش‌نیاز است. |
| تحلیل بنیادی و رویدادها | خودکار پس از ساخت ایجنت | به ماژول اختصاصی و منابع رسمی صورت‌های مالی، افشا و صنعت نیاز دارد. |
| تحلیل کل پرتفوی | خودکار پس از ساخت ایجنت | نیازمند وزن نمادها، صنعت، نقدشوندگی، دادهٔ ریسک و تاریخچه است. |
| گزارش فارسی | خودکار پس از ساخت مدل جمع‌بند | گزارش باید واقعیت، محاسبه، برداشت و کمبود داده را تفکیک کند. |
| خرید، فروش، برداشت یا انتقال وجه | خارج از دامنه | هیچ مسیر فنی برای اجرای اقدام مالی در پروژه ایجاد نمی‌شود. |

گردش کار هدف: زمان‌بند محلی ← دریافت از مرورگر و منابع تأییدشده ← اعتبارسنجی ← ایجنت تابلو، تکنیکال، بنیادی، رویدادها و ریسک ← مدل جمع‌بند جک ← ذخیره و نمایش در index.html.

### تقویم و زمان‌بندی بورس ایران

برای بازار سهام بورس و فرابورس ایران، زمان‌بندی پایهٔ جک شنبه تا چهارشنبه، جلسهٔ اصلی معاملات از ۰۹:۰۰ تا ۱۲:۳۰ به منطقهٔ زمانی `Asia/Tehran` است. پیش‌گشایش حدود ۰۸:۴۵ و مرحلهٔ معاملات پایانی برای برخی نمادها ۱۲:۴۵ تا ۱۳:۰۰ است. این زمان‌ها باید در تنظیمات قابل‌اصلاح باشند؛ تعطیلات رسمی، توقف نماد، اطلاعیهٔ بازار و تغییر مقررات بر برنامهٔ پایه مقدم‌اند.

زمان‌بند فقط در پنجره‌های تعیین‌شده تحلیل تازه و پایش بازار را اجرا می‌کند. بیرون از ساعت بازار، آخرین دادهٔ بازار «زنده» تلقی نمی‌شود؛ جک فقط گزارش‌های قبلی را نمایش می‌دهد یا برای اجرای بعدی آماده می‌شود. در روز تعطیل یا وقتی تقویم/وضعیت بازار نامشخص است، اجرای تحلیلی با `MARKET_CLOSED` یا `DATA_BLOCKED` ثبت می‌شود.

محدودیت‌های پیش‌بینی‌شده: تغییر ساختار سایت، CAPTCHA، ورود دومرحله‌ای، قطع اینترنت و دادهٔ ناقص می‌توانند اجرای خودکار را متوقف کنند. در این شرایط وضعیت خطا یا `DATA_BLOCKED` گزارش می‌شود. API نداشتن سامانهٔ بورسی فقط به روش دریافت پرتفوی مربوط است و روش اجرای مدل هوش مصنوعی را تعیین نمی‌کند.

### ایجنت کنترل نمودار و اندیکاتور

یک ایجنت مستقلِ کنترل نمودار به معماری افزوده می‌شود. پس از ورود دستی کاربر، این ایجنت نماد، تایم‌فریم و اندیکاتور درخواستی را در سایت نموداریِ تأییدشده باز و تنظیم می‌کند، پارامترهای اعمال‌شده را ثبت می‌کند، داده یا تصویر قابل‌مشاهده را استخراج می‌کند و نتیجه را به ایجنت تکنیکال می‌فرستد.

منوی سایت، نام اندیکاتور، پارامترها و زمان ثبت‌شده بخشی از ردگیری تحلیل هستند. اگر اندیکاتور در سایت موجود نبود، نمودار خوانا نبود یا مقدار استخراج‌شده با محاسبه از دادهٔ OHLCV سازگار نبود، ایجنت باید وضعیت `DATA_BLOCKED` یا اختلاف داده اعلام کند. برای دادهٔ تاریخی معتبر، محاسبهٔ مستقل اندیکاتور در هستهٔ پایتون مرجع کنترل نتیجه است.

ایجنت فقط می‌تواند در سایت‌های مجاز روی نماد، تایم‌فریم و اندیکاتورهای تعریف‌شده کار کند. ورود، CAPTCHA و احراز هویت دومرحله‌ای توسط کاربر انجام می‌شود. این ایجنت هیچ‌گونه سفارش، خرید، فروش، برداشت یا انتقالی انجام نمی‌دهد.

## قاعدهٔ نتیجهٔ نهایی و پلهٔ محدود

نظر نهایی جک برای هر نماد برآیند شواهد ایجنت‌های تابلو، تکنیکال، بنیادی، رویدادها و ریسک است؛ جمع سادهٔ رأی‌ها نیست. خروجی کاربرپسند برای هر نماد یکی از چهار حالت «خرید آزمایشی»، «فروش آزمایشی»، «نگهداری / واچ‌لیست» یا «عدم اعلام نظر» است. حالت آخر در دادهٔ ناقص، قدیمی، متناقض یا ناکافی اجباری است.

خرید یا فروش فقط یک پلهٔ محدود دارد. جک حداکثر اندازهٔ همان پله را بر اساس بودجهٔ ریسک، فاصله تا حد زیان، کارمزد، لغزش، سرمایهٔ آزاد، محدودیت تمرکز و نقدشوندگی محاسبه و نمایش می‌دهد. افزایش خودکار موقعیت، سفارش چندپله‌ای، میانگین کم‌کردن و اجرای مالی واقعی خارج از دامنه است. نتیجه شامل شرط تأیید، شرط ابطال، شواهد موافق و مخالف، منبع و زمان داده و تأیید انسانی است.
